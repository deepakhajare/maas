DEFAULT menu
PROMPT 0
MENU TITLE {{title}}
TIMEOUT 1
ONTIMEOUT execute

LABEL execute
        KERNEL {{kernel|relpath}}
        INITRD {{initrd|relpath}}
        MENU LABEL Continue
        APPEND {{append}}
        IPAPPEND 2

MENU end
